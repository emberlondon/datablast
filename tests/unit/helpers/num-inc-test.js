import {
  numInc
} from '../../../helpers/num-inc';
import { module, test } from 'qunit';

module('NumIncHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numInc(42);
  assert.equal(result, 43);
});
