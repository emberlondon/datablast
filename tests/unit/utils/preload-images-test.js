import preloadImages from '../../../utils/preload-images';

QUnit.module('preloadImages');

// Replace this with your real tests.
QUnit.test('it works', function(assert) {
  var result = preloadImages();
  assert.ok(result);
});
