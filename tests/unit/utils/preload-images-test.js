import preloadImages from '../../../utils/preload-images';
import { module, test } from 'qunit';

module('preloadImages');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = preloadImages();
  assert.ok(result);
});
