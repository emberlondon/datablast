{{#db-slide}}
  {{#code-block language="javascript"}}
  var say = function(...words) { return words.join(' '); };

  var salute = say.bind(context, "Hello");

  var geekSalue = salute.bind(context, "world");

  var geekSaluteTomster = salute.bind(context, "Tomster!");

  geekSaluteTomster(); // "Hello world Tomster!"
  {{/code-block}}
{{/db-slide}}

